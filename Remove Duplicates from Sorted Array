def removeDuplicates(nums,n):
    # Write your code here.
    p1 = 0
    for i in range(1,len(nums)):
        if nums[i] != nums[p1]:
            p1 += 1
            nums[p1] = nums[i]
    return p1 + 1 

pass
